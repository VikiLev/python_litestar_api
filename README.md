# 🧪 Litestar Test API

Невеликий приклад REST API на основі **Litestar** (Python 3.12, SQLAlchemy, Docker).  
Проєкт демонструє базові CRUD-операції з таблицею `TestAPI`.

---

## 🚀 Запуск

1. Клонувати репозиторій:
   ```bash
   git clone <your-repo-url>
   cd python_litestar_api
   
2. Запустити застосунок через Docker:

    ```bash
    docker-compose up --build
   
3. Після запуску API буде доступне за адресою:

http://127.0.0.1:5000

📚 Ендпоїнти
Метод	URL	Опис

GET	/health	Перевірка стану сервісу

GET	/schema	OpenAPI-документація

GET	/tests/	Отримати список усіх записів з табл тест

GET	/tests/{id}	Отримати запис тест за ID

POST	/tests/	Створити новий запис тест

PUT	/tests/{id}	Оновити запис тест

DELETE	/tests/{id}	Видалити запис тест


